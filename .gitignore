*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx/
local.properties

# Models are downloaded/side-loaded at runtime, never committed
/models/*.gguf
/models/*.onnx
/models/*.tar.bz2
.kotlin/

# Linux desktop port build artifacts (live on the linux branch; keep off main)
/desktop/
/shared/
