package studio.voxsum.desktop

/** App metadata shown in the About section. Keep VERSION in sync with
 *  desktop/build.gradle.kts `packageVersion` on each release. */
object AppInfo {
    const val VERSION = "0.3.0"
}
